
1. 刪除包含某字串的整行
    :[range]g/pattern/[cmd]
    [range]表示範圍，或用直接用 g 表示 globe
    [cmd] 使用 "d" 就可以刪除整行

This replaces with newline:
sed "s/regexp/`echo -e '\n\r'`/g"

g/xxx/d，删除包含xxx的行
v/xxx/d，删除不含xxx的行
:%s/xxx//gn，统计xxx个数，n表示只报告匹配的个数而不进行实际的替换。


使用shell sort排序数字
:1,$!sort -n
